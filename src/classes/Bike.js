export default class Bike {

    constructor(id, title, height, folder, colors, wheel, color = 'blackblue') {

        this.id = id;
        this.title = title;
        this.height = height;
        this.folder = folder;
        this.colors = colors;
        this.wheel = wheel;
        this.color = color;

    }

};